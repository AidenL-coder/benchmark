# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
venv/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Experiment outputs (raw logs are large; regenerate via configs)
runs/
outputs/
wandb/
*.log

# Benchmark data (fetched, not vendored -- see `cbs data status`)
data/raw/
data/cache/

# Local overrides
configs/local/
.env

# Raw per-task run logs from Polyglot arms (D-44/D-45) -- tens of MB of
# container/pip output. The per-arm results JSON in results/ is the actual
# evidence and IS tracked; these are kept locally for forensics only.
results/logs/
